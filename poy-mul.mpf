program polyMul;
architecture shared (10);
const n=2;
var a, b : array[0..n] of real;
c: array[0..2*n];
i, j: integer;

procedure mul(i, j: integer);
begin
	c[i+j] := c[i+j] + a[i] * b[j];
end;

begin
	forall i:=0 to n do
		forall j:=0 to n do
			mul(i, j);
end.